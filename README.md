# translate-shell.vim
A vim plugin of translate-shell

## Install translate-shell
### Ubuntu/Debian
```
sudo apt update && sudo apt install translate-shell -y
```

### MacOS
```
brew install translate-shell
```

## Install this plugin (vim-plug)
```

```
